type FundingToken @entity {
  id: ID!
  owner: ID!
  fundingvalue: BigInt!
  tenor: BigInt!
}

type Project @entity {
  id: ID!
  name: ID!
  projectAddress: ID!
  ownerAddress: ID!
  bidderAddress: ID!
  auditorAddress: ID!
}
